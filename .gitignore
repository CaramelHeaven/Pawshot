# Tuist generation artifacts
*.xcodeproj
*.xcworkspace
Derived/
.build/
.tuist-bin/
.tuist-version

# Xcode
*.xcuserdatad
xcuserdata/
DerivedData/
# `tuist xcodebuild build` drops object files and .pcm here — close to two hundred megabytes
build/

.DS_Store

# Personal signing (Team ID) — see Config/Signing.local.xcconfig.example
Config/Signing.local.xcconfig

# Claude Code: the shared SwiftFormat hook stays, anything personal stays local
.claude/*
!.claude/settings.json
